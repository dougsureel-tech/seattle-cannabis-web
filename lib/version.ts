// Build identity exposed in the footer so Doug can verify a deploy landed.
// Bump BUILD_VERSION manually for major UX/feature changes; the short SHA
// comes from Vercel automatically on every deploy and is the authoritative
// "did my push actually land" signal.

// 4.150 — `/menu` server-rendered closure banner. Extends v4.140's `/order` banner to the higher-traffic `/menu` (iHeartJane Boost embed) entry point. New `<ClosureBanner>` component (presentational, used by both `/menu` server-side + `/order` client-side via prop). `/menu` page.tsx now does `Promise.all([getActiveDeals, fetchClosureStatus])` server-side; banner renders above the heading + JaneMenu when closed. ISR 60s on the page bounds the fetch frequency. Same graceful-degrade behavior — endpoint unreachable = banner doesn't render. Mirror on greenlife-web v3.240.
// 4.145 — `/brands` index deletion code lands (v4.135 changelog described this; that entry shipped via the closure-banner agent's bundle but the code edits were still uncommitted in working tree). This commit lands the actual file deletions + nav-strip + breadcrumb edits.
// 4.140 — `/order` block-on-closure banner. Fetches inventoryapp `/api/public/closure-status` on mount + before-submit re-check inside `placeOrder()`. When manager has flagged today closed via `/admin/hours-override`, surfaces an amber banner with the reason text, disables the Place Order button (label flips to "Closed today"), and the before-submit guard prevents a stale-state race. New `lib/closure-status.ts` helper wraps the fetch with a 5s AbortController timeout + always returns `{isClosed:false}` on any error path so a flaky endpoint never silently blocks ordering during normal hours. Default endpoint base = `seattle-cannabis-co.vercel.app`, overridable via `NEXT_PUBLIC_INVENTORYAPP_URL` (https-only). Closes the visibility chain queued in claude-loop-grinder Batch A items 1+2 (admin override v9.395 → public endpoint v9.745 → public-site banner now). Mirror on greenlife-web v3.230.
// 4.135 — `/brands` index page deleted (Doug + Kat call 2026-05-04). Even after the v4.101/v4.107/v4.119/v4.125 filter chain locked the data down to "products we currently carry + sold within 365 days," Kat's reasons for deletion were structural, not data-quality: (a) the page exposed our full vendor relationship list to outsiders; (b) it made daily-deal vendor concentration glanceable. Boutique `/brands/[slug]` overrides KEPT — they pull SEO traffic for "NWCS Seattle" / "Mfused Rainier" etc. queries. **Touched:** index → 308 redirect to `/menu`; SiteHeader nav, SiteFooter, home `Top Brands` section (kept section but stripped index-link affordances), about + visit page mesh tiles, not-found suggestion list, sitemap, llms.txt + llms-full.txt, vendor-access thanks page, blog post body. **`/brands/[slug]` template:** dropped "← All Brands" footer backlink + dropped "All Brands /" top-of-hero breadcrumb + dropped the "Brands" rung from the BreadcrumbList JSON-LD. Per-brand override components (NWCS, Mfused, Avitas, etc.) intentionally NOT touched. Mirror on greenlife-web v3.225.
// 4.125 — Brand-leakage round 3 — sales-history gate extended to product surfaces (/order, /shop, /stash, home featured carousel, home category previews, brand-products grid). Round 2 (v4.119) only gated brand pages; products from leaked brands could still appear on /menu+/order+/shop because those don't go through getActiveBrands/getBrandBySlug. Now `getMenuProducts` + `getProductsByIds` + `getFeaturedProducts` (+ fallback) + `getCategoryPreviewProducts` + `getBrandProducts` all INNER JOIN `brands_with_recent_sales` (vendors with ≥1 sale_line_items.sold_at >= NOW() - INTERVAL '365 days'). Customer-facing product display surfaces uniformly gated. Mirror on greenlife-web v3.211.
// 4.119 — Brand-leakage round 2 — sales-history gate (Doug screenshot 2026-05-04: "ABS Buds" Wenatchee-era brand rendering on Seattle with 13 FOG concentrate SKUs and "13 products in Seattle, WA" hero). Root cause: Seattle's Neon has leaked inventory_snapshots from a seed migration; v4.107 qty>0 alone couldn't distinguish "real Seattle inventory" from "ghost from a Wenatchee-seeded product." `getActiveBrands` + `getBrandBySlug` now INNER JOIN `brands_with_recent_sales` CTE that picks vendor_ids with ≥1 `sale_line_items.sold_at >= NOW() - INTERVAL '365 days'` row. Brands with no tail sale at THIS store get hidden from /brands index AND 404 on direct `/brands/[slug]` (so hand-authored Wenatchee-only override components don't render either). Mirror on greenlife-web v3.205.
// 4.113 — `proxy.ts` /api/health exemption broadened from exact-equality to `startsWith("/api/health")` so the cheap second-bucket /api/health/ping liveness endpoint also stays reachable on apex / per-deployment Vercel URLs. Pre-fix: /api/health was already exempt but /api/health/ping was still 308'ing — uptime monitors hitting /ping every 30-60s for second-bucket liveness couldn't follow the redirect cleanly. Same fix on greenlife-web v3.199.
// 4.107 — Brand pages + featured + category previews now require current inventory (Doug bug report 2026-05-04 "showing items we hant had in a long time on both stores"). Layers on top of v4.101/v4.102 carry_status tightening. Now `getActiveBrands` + `getBrandBySlug` + `getBrandProducts` + `getFeaturedProducts` (+ fallback) + `getCategoryPreviewProducts` all join the canonical `latest_inv` CTE and filter `li.qty > 0`. Customer no longer sees brands that haven't shipped a SKU in months, or "featured" products with empty shelves. WSLCB advertising-accuracy + customer-trust fix. Mirror on greenlife-web v3.187.
// 4.102 — `/order` + `/shop` + `/stash` product filter tightened (Doug bug report 2026-05-04 "saying we have things that we dont"). `getMenuProducts` + `getProductsByIds` in `lib/db.ts` now require strict `carry_status = 'active'` AND INNER JOIN to a latest-snapshot `qty > 0` CTE. Pairs with v4.101 brand-page tightening: same root cause, different surfaces. Mirror on greenlife-web v3.182.
// 4.101 — `/brands/[slug]` + `/brands` tightened product filter (Kat bug report 2026-05-04). `getActiveBrands` + `getBrandBySlug` + `getBrandProducts` in `lib/db.ts` now require `carry_status = 'active' AND unit_price > 0` instead of the looser `carry_status != 'discontinued'`. Kat: Bon Bombs were rendering on the Seattle brands page despite never having been carried in Seattle; samples were rendering on both stores. The looser filter let phasing-out vendors + $0-priced placeholder rows through. Same change shipped to greenlife-web v3.181. SQL stays one query each, no N+1 introduced.
// 4.96 — Apply form `?position=` deep-link support — when /careers links to /apply?position={id}, form fetches position from inventoryapp API, pre-selects role + shows 'Applying for: {title}' pill at top. Hidden positionId field passes the link to /api/applications. Silent fallback when fetch fails or position no longer open.
// 4.91 — Public /careers landing (Hiring Phase 5). Fetches open positions from inventoryapp /api/positions/open?store=seattle with 5-min ISR + 5s fetch timeout (falls back to "we keep good resumes on file" empty state on any failure). Position cards: title, store badges, pay-range/hours-pattern chips, safe-markdown description (XSS-safe — paragraphs/bullets/**bold** only, no HTML pass-through), "Apply for this role" CTA deep-linking /apply?position={id}. Hero leans on the "since 2010, on Rainier since 2018" heritage angle. Indigo/violet theme matching SCC chrome. Mirror on greenlife-web v3.171.
// 4.86 — Public /vendor-access self-serve form for brand partners (PLAN_VENDOR_ASSETS_PORTAL path B). Lands at /vendor-access — collects company + contact + email + phone + brand confirmation + intent — POSTs JSON to inventoryapp /api/vendor-access — feeds existing /admin/vendor-access-requests admin queue. Indexed (canonical), thanks page noindex. Free-text only — no file upload. Mirror page on greenlife-web v3.166.
// 4.81 — /brands/[slug] generic-template renders vendor-authored brand bio + Instagram/X/Facebook handles when filled in via /vmi/profile (inventoryapp). Section sits above the order CTA, only renders when at least one field is non-null. Handles are sanitized to /^[A-Za-z0-9._-]+$/ before being concatenated into URLs (prevents query-param injection or path traversal). Per-brand override components intentionally NOT touched — those are graduated, hand-authored layouts.
// 4.76 — /apply personality prompts: two optional written prompts (product-recommendation pitch + customer-recovery story) capture personality signal without the photo discrimination risk. Stored in applicants.metadata JSONB on inventoryapp side. Compliance: written-only — no photo (WA RCW 49.60 / EEOC pre-offer photo discrimination risk).
// 4.71 — Public /apply form: apply-to-work intake with resume upload + 3 references + 21+ confirmation. POSTs to inventoryapp /api/applications. Compliance: no photo / no SSN / no DOB.
export const BUILD_VERSION = "4.150";

export const BUILD_SHA = (
  process.env.VERCEL_GIT_COMMIT_SHA ??
  process.env.NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA ??
  "dev"
).slice(0, 7);
